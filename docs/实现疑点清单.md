# 实现疑点清单

> **版本**：v0.3.4 对应
> **日期**：2026-08-05
> **目的**：供程序/策划在阅读设计文档后标注所有"实现存疑"的点，集结后统一解答
> **填写说明**：
> - 状态：`待解答` / `已解答` / `延后讨论`
> - 提出人：填写提出者姓名
> - 解答：由设计负责人填写

---

## 一、机制实现类

### Q1. 「每轮至多 1 次定身」的精确实现

| 项目 | 内容 |
|------|------|
| 设计原文 | 深渊水母王进化后特性：「每轮至多 1 次 50% 暴击定身（0.6 秒）」 |
| 潜在问题 | 1. 「一轮」如何定义？是一次完整的弹幕发射周期（8 道弹幕全部发射完毕）？还是固定时间窗口（如每 1 秒）？<br>2. 如果是多弹道武器，如何判定"触发定身"的时机？<br>3. 定身冷却如何实现？是全局计时器还是每轮重置？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **「一轮」定义为 1 秒固定时间窗口**。实现方式：<br>1. 武器拥有 `定身冷却 = 1.0s` 计时器，初始为 0；<br>2. 每次弹幕命中敌人时，若 `冷却 <= 0` 且命中为暴击（50% 概率），则触发定身（0.6s）并将冷却重置为 1.0s；<br>3. 无论本秒内命中多少敌人，最多触发 1 次定身；<br>4. 精炼后弹幕数增加不改变定身触发逻辑，仅增加命中次数（即提高触发概率）。 |

---

### Q2. 链式闪电「伤害递减 40%/跳」的计算方式

| 项目 | 内容 |
|------|------|
| 设计原文 | 风暴之锚进化后特性：「链式闪电传导（2 次跳转，伤害递减 40%/跳）」 |
| 潜在问题 | 1. 递减是加法还是乘法？例：第一跳 100%，第二跳 60%，还是 100%→60%→36%？<br>2. 递减基数是什么？是原始伤害还是上一跳伤害？<br>3. 精炼后链跳数增加，递减比例是否调整？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **相对上一跳乘算**：第 1 跳 100%，第 2 跳 ×0.6（即递减 40%）。湮灭雷暴连锁为 ×0.7/次。精炼增加跳数时**仍用同一衰减系数**，不另调（除非专项平衡补丁）。见主文档附录 A.1（v0.3.1）。 |

---

### Q3. 「大范围震荡波（约屏幕 70%）」的范围定义

| 项目 | 内容 |
|------|------|
| 设计原文 | 星海锚坠进化后特性：「3 秒 CD 大范围震荡波（40% 伤害 + 0.5 秒眩晕）」 |
| 潜在问题 | 1. "屏幕 70%"的具体像素单位是多少？是否随分辨率变化？<br>2. 是否需要动态计算屏幕尺寸？还是固定一个常量范围（如半径 400 单位）？<br>3. 地图边缘是否有边界检测？震荡波是否被阻挡？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **采用固定单位半径，圆心为玩家**（非灯塔）。具体：<br>1. 震荡波范围 = 以**玩家**为中心的半径 **400 游戏单位**（约主视野 70%）；<br>2. 不随分辨率缩放；<br>3. **不受地形阻挡**；<br>4. 氛围边界约 600，有效作战见主文档 §5.2。已回写附录 A.3 / B.10。 |

---

### Q4. 百分比伤害「每秒 2% 最大生命」的结算机制

| 项目 | 内容 |
|------|------|
| 设计原文 | 星海锚坠精炼 III：「锚压」每秒 **1%** 敌人最大生命伤害，持续 3 秒（红线上限 ≤2%/秒；旧稿 2% 已废） |
| 潜在问题 | 1. 结算是每帧还是每秒一次性结算？<br>2. 百分比伤害是否受减伤/护甲影响？是否视为真实伤害？<br>3. 多个锚压叠加时如何处理？<br>4. 对 Boss 的实际压力？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | 数值口径：**1%/秒 × 每次持续 3s**。建议每 **0.25s** tick 结算 0.25%（共 12 tick/次）。无视护甲/抗性，但受 Boss 霸体/无敌帧跳过。同目标不叠层：刷新取较长剩余。稳态覆盖率 ≈ min(1, 3/CD)；精炼 I 后 CD=2.5s → 覆盖约 **100%**，则 120s 战约 **≤120%** 最大生命（有无敌则更低）。**禁止**在未计入覆盖率时写死「1%×120s」。见 `tools/refine_dps_simulation.py`。 |

---

### Q5. 「减速叠加」的计算方式

| 项目 | 内容 |
|------|------|
| 设计原文 | 永夜潮汐进化：「全屏 50% 减速」+ 精炼 III：「潮湿 Buff 减速 15%」 |
| 潜在问题 | 1. 减速是加法叠加（50% + 15% = 65%）还是乘法叠加（1 - (1-0.5)×(1-0.15) = 57.5%）？<br>2. 设计文档提到「双重减速叠加后可行动时间仍 ≥ 30%」，如何验证？<br>3. 减速上限是多少？是否有硬性约束？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **乘法叠乘**：有效移速系数 = Π(1 - 减速%)。验收：采样 30s，`被控时长/30 ≤ 0.70`（可行动 ≥30%）。建议硬顶：单一来源减速 ≤60%，叠乘后有效移速系数 ≥0.25（即总减速效果不超过 75% 等效）。见主文档 §6.6 / v0.3.1。 |

---

## 二、数值实现类

### Q6. 经验曲线公式的浮点数精度

| 项目 | 内容 |
|------|------|
| 设计原文 | `E(n) = floor(22 × 1.11^(n-1) + 3×(n-1))` |
| 潜在问题 | 1. `1.11^(n-1)` 在高等级时是否会产生浮点精度误差？<br>2. 是否需要预先计算并缓存所有 30 级的经验表？<br>3. 公式实现是用脚本生成表还是运行时动态计算？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **采用脚本预生成表，禁止运行时动态计算**：<br>1. 由 `tools/generate_exp_table.py` 脚本预计算所有 30 级经验值，输出为常量表；<br>2. 高等级浮点精度误差在 `floor` 取整后不影响结果（验证：`1.11^29` 的浮点误差 < 0.01，远小于 1 的取整阈值）；<br>3. 游戏运行时直接查表 `EXP_TABLE[level]`，零额外计算开销；<br>4. 调参仅改脚本常量后重新生成，**禁止手改表中任意一行**（红线）。 |

---

### Q7. 伤害公式中乘区的计算顺序

| 项目 | 内容 |
|------|------|
| 设计原文 | `最终伤害 = 基础伤害 × 角色系数 × 武器等级系数 × Σ(同乘区加成) × Π(跨乘区加成) × 暴击(×1.8) × 减伤效果` |
| 潜在问题 | 1. 计算顺序是否严格执行？例如：是先乘暴击再减伤，还是先减伤再乘暴击？<br>2. 「同乘区加算」的具体定义是什么？例如「攻速」乘区内，所有攻速加成是加法叠加？<br>3. 软上限的衰减公式是什么？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | 顺序：基础 → 角色 → 武器等级 → 同乘区和 → 跨乘区依次乘 → 暴击 → 目标减伤。同乘区=同一属性条加算。软上限见 Q8 / 主文档 §6.9（v0.3.1）。 |

---

### Q8. 软上限的精确实现方式

| 项目 | 内容 |
|------|------|
| 设计原文 | 属性软上限表：攻速 +150%、移速 +60%、范围 +120%、暴击率 60% 等 |
| 潜在问题 | 1. 软上限是线性衰减还是硬截断？例：攻速加成超过 150% 后，是每点收益降为 50%？还是完全无效？<br>2. 暴击率「之后每点收益减半」是指超过 60% 后，每 1% 暴击只提供 0.5% 实际暴击率？<br>3. 减伤的递减公式 `1 - 1/(1 + 0.02×数值)` 如何在代码中实现？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | 攻速/移速/范围/经验：超过软上限后**额外部分按 50% 计入**（非硬截断）。暴击：>60% 后面板每 1% → 实际 0.5%。减伤：`min(0.70, 1 - 1/(1+0.02×减伤点数))`，点数为加算输入。见主文档 §6.9。 |

---

### Q9. 敌人数值公式的区域系数

| 项目 | 内容 |
|------|------|
| 设计原文 | `敌人生命 = 基础生命 × 区域系数 × (1 + 0.10 × 夜数) × (1 + 0.15 × ⌊夜数/5⌋)` |
| 潜在问题 | 1. 「区域系数」是什么？文档未明确定义。<br>2. 是否存在多个区域？还是区域系数始终为 1.0？<br>3. 如果有多个区域，区域如何定义？地图分块？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | MVP（第一章灯塔海域）**恒为 1.0**。与地图内礁石/浅滩分块无关。正式版第二章永冻湾预留 **1.15**。见主文档 §8.2（v0.3.1）。 |

---

## 三、性能优化类

### Q10. 350~450 同屏敌人的碰撞检测优化

| 项目 | 内容 |
|------|------|
| 设计原文 | 性能预算：同屏敌人 350~450（PC 60fps）；敌人 AI 分帧更新 |
| 潜在问题 | 1. 碰撞检测是使用空间哈希还是四叉树？<br>2. 敌人↔敌人只做分离不做物理，具体实现方式？<br>3. 投射物「空间哈希」的网格尺寸是多少？<br>4. 分帧更新的优先级如何排序？近战敌人优先还是随机？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **采用空间哈希网格**（非四叉树，因场景为 2D 平面且敌人分布较均匀）：<br>1. 网格尺寸 = **敌人半径 × 4**（约 80 游戏单位），确保每个网格内敌人 ≤ 10 个；<br>2. 敌人↔敌人**仅做分离检测**（防重叠），不做物理碰撞/伤害；使用简单排斥向量（`O(n²)` 仅在同网格内）；<br>3. 投射物与敌人碰撞：使用**同一空间哈希网格**，投射物半径 ≤ 10，每帧仅检测所在网格及 8 邻格；<br>4. AI 分帧优先级：**远程敌人（需要持续更新目标位置）> 近战敌人（简单寻路）> 小怪（最简行为）**。实现：将敌人列表按优先级分组，每帧更新远程组、每 2 帧更新近战组、每 4 帧更新小怪组。 |

---

### Q11. 精炼 III 级持续生成物的性能影响

| 项目 | 内容 |
|------|------|
| 设计原文 | 精炼 III 级部分效果：雷云持续伤害、毒水母群每秒生成、临时精英召唤物等 |
| 潜在问题 | 1. 深渊水母王精炼 III「每秒生成 2 只毒水母」在 60 秒战斗中是否会产生 120 个实体？<br>2. 这些临时实体是否计入同屏上限？<br>3. 雷云的持续伤害判定是每帧还是每秒？<br>4. 临时精英召唤物的 AI 复杂度如何？是否需要完整 AI？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **MVP 阶段精炼 III 不实现**（仅 I/II），以下为设计预留方案：<br>1. 毒水母等临时实体**计入同屏上限**，但采用 **TTL（生存时间）+ 生成速率** 双重限制：如每秒生成 2 只、存活 10 秒，稳态最大 20 只，不构成性能风险；<br>2. 雷云持续伤害判定为**每 0.5s tick**（非每帧），仅检测雷云覆盖范围内的敌人并扣除伤害；<br>3. 临时实体**使用简化 AI**（寻路+攻击），不具备完整状态机；<br>4. 若同屏实体接近上限（400+），临时实体生成自动降频至 1 只/秒。 |

---

### Q12. 粒子系统「200 个以内」的精确定义

| 项目 | 内容 |
|------|------|
| 设计原文 | 渲染预算：粒子（血花/光效）总量预算 200 个以内 |
| 潜在问题 | 1. "200 个粒子"是指粒子发射器数量还是粒子实例数量？<br>2. 血花/光效的粒子池是否共用 200 上限？还是分别 200？<br>3. 超过上限时是丢弃新粒子还是回收旧粒子？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **200 指粒子实例数量**（非发射器），所有粒子**共用一个全局池**：<br>1. 血花、光效、爆炸、进化特效等所有粒子共用 200 实例上限；<br>2. 使用**对象池 + 优先级回收**策略：<br>　- 高优先级（进化特效、Boss 技能）：立即分配，不限制数量<br>　- 中优先级（爆炸、大型伤害）：正常分配<br>　- 低优先级（血花、小型光效）：超出上限时**不生成**新粒子，保留旧粒子直到自然过期<br>3. 实现建议：`ParticleManager` 维护 `max_particles=200` 计数器 + 按优先级排序的对象池，每帧统一回收过期粒子。 |

---

## 四、UI/UX 类

### Q13. 抉择之昼「无强制倒计时」的具体实现

| 项目 | 内容 |
|------|------|
| 设计原文 | 抉择之昼：无强制倒计时，可购买、升级、重铸、触发事件，也可立即跳过。UI 软提示「建议用时 20 秒」 |
| 潜在问题 | 1. "软提示"是指 UI 显示倒计时还是仅文字提示？<br>2. 超过约 40 秒"可轻提示"是指弹窗还是 toast？<br>3. 是否有硬性超时限制？还是真的无限等待？<br>4. 跳过按钮的默认焦点位置？键盘快捷键？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **采用「软提示 + 无限停留」方案**，详见 [单局时长控制策略.md](单局时长控制策略.md)：<br>1. 进入抉择之昼时显示「💡 建议用时：约 20 秒」淡灰色文字（右下角，不抢眼）；<br>2. 停留超过 40 秒时弹出 Toast 提示「可随时跳过进入下一夜」（3 秒后消失，非弹窗不打断操作）；<br>3. **无硬性超时**，可无限停留；停留超过 90 秒不再提示（避免催促感）；<br>4. 跳过按钮「立即进入下一夜」**默认获得焦点**，支持 Space/Enter/Esc 快捷键；<br>5. 应急预案详见 §4（如时长超标可缩短战斗时长、优化 UI 默认选项等）。 |

---

### Q14. 升级三选一的权重保底实现

| 项目 | 内容 |
|------|------|
| 设计原文 | 权重保底：连续 2 次无武器 → 第 3 次必出武器；连续 3 次无某被动系 → 第 4 次该系权重 ×3 |
| 潜在问题 | 1. 保底计数器是全局的还是每局独立的？<br>2. "必出武器"是三选一全为武器？还是至少 1 个武器？<br>3. 权重 ×3 是临时提升还是永久提升？<br>4. 计数器何时重置？出现对应选项后立即清零？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **保底计数器每局独立**（局外不继承）：<br>1. 武器保底：连续 2 次三选一**无任何武器** → 第 3 次三选一**至少 1 个选项必为武器**（非全为武器，保持构筑自由度）；<br>2. 被动系保底：连续 3 次三选一**无该系被动** → 第 4 次该系所有被动权重**临时 ×3**（仅本次三选一有效，出后清零）；<br>3. 计数器重置规则：<br>　- 武器保底：出现武器选项 → 立即清零<br>　- 被动系保底：该系任意被动出现 → 该系计数器清零<br>4. 保底计数器在一局开始时全部重置为 0。 |

---

### Q15. 死亡原因可视化的具体呈现

| 项目 | 内容 |
|------|------|
| 设计原文 | 死亡原因可视化：结算页说明死因与建议 |
| 潜在问题 | 1. 死因统计是仅统计最后 1 次伤害来源，还是统计整局伤害构成？<br>2. "建议"是固定文本还是动态生成？<br>3. 数据来源是实时统计还是事后分析？<br>4. 是否需要记录完整的伤害日志？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **采用「最后一击 + 整局构成」双重呈现**：<br>1. 死因统计：<br>　- **主死因**：最后 10 秒内造成最大伤害的敌人类型（如「被爆炸贝自爆击杀」「被渊潮长枪兵冲锋击杀」）<br>　- **辅助信息**：整局伤害构成 Top 3 敌人类型 + 死亡时所在夜数<br>2. 建议文本：**动态生成 + 模板化**：<br>　- 模板示例：「建议提升防御类被动或增加走位闪避」「下一局考虑选择铁匠提高生存」<br>　- 根据死因类型匹配建议模板（如死于高伤害→建议防御，死于控制→建议解控）<br>3. 数据来源：**实时统计**（内存中维护伤害统计器，不写日志文件）<br>4. 不记录完整伤害日志（性能考虑），仅聚合统计（按敌人类型分组的累计伤害）。 |

---

## 五、网络/存档类

### Q16. 确定性随机的种子同步机制

| 项目 | 内容 |
|------|------|
| 设计原文 | 确定性随机：带种子 RNG，支持每日挑战与回放 |
| 潜在问题 | 1. 种子是否需要网络同步？还是本地存储？<br>2. 每日挑战的种子如何下发？服务器时间戳？<br>3. 回放功能是否需要完整记录玩家操作？还是仅记录种子？<br>4. 不同平台的浮点数精度是否会影响确定性？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **MVP 阶段仅本地种子，每日挑战延后**：<br>1. 种子来源：<br>　- 普通局：随机生成 32 位种子，本地存储<br>　- 每日挑战（抢先体验）：服务器下发当日固定种子 + 指定角色<br>2. 回放机制：<br>　- MVP 仅记录种子，不记录操作<br>　- 抢先体验版回放：完整记录玩家操作序列（移动轨迹 + 拾取时间点），回放时重放输入即可<br>3. 跨平台确定性：<br>　- 使用 **整数运算 RNG**（如 Xoshiro256** 或 PCG），避免浮点误差<br>　- 所有随机决策（敌人位置、掉落、三选一）均使用整数 RNG 采样<br>　- 同一种子 + 同一版本 = 完全一致的游戏状态<br>4. 种子持久化：存档中记录种子值，支持重放。 |

---

### Q17. 存档数据的版本兼容性

| 项目 | 内容 |
|------|------|
| 设计原文 | 存档：JSON（Steam 云同步）——解锁、灯塔等级、星尘、设置 |
| 潜在问题 | 1. 存档格式是否有版本号？<br>2. 新增内容后，旧存档如何迁移？<br>3. 云同步的冲突解决策略是什么？时间戳覆盖？手动选择？<br>4. 是否需要存档校验（防作弊）？ |
| 提出人 | （待填写） |
| 状态 | `已解答` |
| 解答 | **版本化存档 + 兼容迁移方案**：<br>1. 存档格式：<br>　- 根字段包含 `"version": 1`（整数版本号）<br>　- 数据结构：`{version, saves: [{character, level, stardust, unlocks, settings, seed, ...}]}`<br>2. 迁移策略：<br>　- 读取存档后检查 `version` 字段<br>　- 若 `version < current_version`，按版本号逐步执行迁移脚本（`v1→v2→v3→...`）<br>　- 示例：新增"精炼数据"时，`v1→v2` 迁移脚本为旧存档默认填充 `refine_level: 0`<br>3. 云同步冲突解决：<br>　- 采用**时间戳 + 手动选择**混合策略<br>　- 默认：时间戳较新的版本覆盖<br>　- 冲突检测：两侧时间戳差 < 5 分钟时弹窗让玩家选择<br>4. 防作弊校验（MVP 简化）：<br>　- 服务端校验：星尘消耗 ≤ 累计获取、解锁条件满足<br>　- 客户端校验：灯塔等级 ≤ 允许上限、单次提升消耗匹配<br>　- 不做 hash 签名（MVP 阶段防作弊优先级低）。 |

---

## 六、待补充问题

> 请在此处新增问题，按上述格式填写

### Q18. （占位关闭）

| 项目 | 内容 |
|------|------|
| 设计原文 | — |
| 潜在问题 | 无新增；请按下方格式追加 Q19+ |
| 提出人 | — |
| 状态 | `延后讨论` |
| 解答 | 占位行，不计入待办。 |
---

## 七、问题统计

| 类别 | 待解答 | 已解答 | 延后讨论 |
|------|--------|--------|---------|
| 机制实现 | 0 | 5 | 0 |
| 数值实现 | 0 | 4 | 0 |
| 性能优化 | 0 | 3 | 0 |
| UI/UX | 0 | 3 | 0 |
| 网络/存档 | 0 | 2 | 0 |
| 占位 | 0 | 0 | 1（Q18） |
| **总计** | **0** | **17** | **1** |

---

## 八、填写指引

### 程序团队关注点

- 机制实现类（Q1-Q5）
- 数值实现类（Q6-Q9）
- 性能优化类（Q10-Q12）
- 网络/存档类（Q16-Q17）

### 策划团队关注点

- 数值实现类（Q6-Q9）
- UI/UX 类（Q13-Q15）

### 建议流程

1. 各团队阅读设计文档后，在此文档中新增问题（按 Q18 格式）
2. 填写「提出人」字段
3. 每周汇总一次，由设计负责人统一解答
4. 解答后更新「状态」和「解答」字段
5. 所有问题解答完毕后，生成「实现规范文档」供开发参考